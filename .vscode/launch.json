{
    // 使用 IntelliSense 了解相关属性。 
    // 悬停以查看现有属性的描述。
    // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Server",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/main.go",
            "cwd": "${workspaceFolder}",
            "args": "${input:args}"
        }
    ],
    "inputs": [
        {
            "id": "args",
            "type": "promptString",
            "description": "The command line arguments",
            "default": "speedtest --derp_region_id 2 --duration 10s"
        }
    ]
}